<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>For You Dilna Joy 💖</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<link href="https://fonts.googleapis.com/css2?family=Pacifico&display=swap" rel="stylesheet">

<style>
body{
  margin:0;
  font-family: Arial, sans-serif;
  background: linear-gradient(135deg,#ffd6e8,#e6f0ff);
  min-height:100vh;
  display:flex;
  align-items:center;
  justify-content:center;
  text-align:center;
  overflow:hidden;
}

.card{
  background:white;
  padding:28px;
  border-radius:28px;
  max-width:420px;
  width:90%;
  box-shadow:0 10px 25px rgba(0,0,0,0.2);
  position: relative;
  z-index: 2;
}

.valentine-title{
  font-family: 'Pacifico', cursive;
  font-size: 28px;
  background: linear-gradient(90deg, #ff3b7a, #ff8fb3, #ff3b7a);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  text-shadow: 0 0 10px rgba(255,59,122,0.4);
  margin-bottom: 10px;
}

h1{color:#ff3b7a;}
p{color:#ff5c8a; font-size:16px;}

.btn{
  padding:12px 22px;
  margin:8px;
  border:none;
  border-radius:25px;
  font-size:16px;
  cursor:pointer;
  background:#ff6f9f;
  color:white;
  transition:0.2s;
}

.btn:hover{transform:scale(1.05);}

.no-btn{
  background:#aaa;
  color:#333;
  position: absolute;
  transition: left 0.25s ease, top 0.25s ease;
}

.page{display:none;}
.active{display:block;}

/* Floating Hearts */
.heart{
  position: fixed;
  bottom: -20px;
  color: #ff5c8a;
  font-size: 20px;
  animation: floatUp 6s linear infinite;
  opacity: 0.8;
  z-index:1;
}

@keyframes floatUp{
  0% { transform: translateY(0) scale(1); opacity: 0; }
  10% { opacity: 1; }
  100% { transform: translateY(-100vh) scale(1.5); opacity: 0; }
}

/* Confetti */
.confetti{
  position: fixed;
  width: 8px;
  height: 8px;
  top: -10px;
  animation: confettiFall 3s linear forwards;
  z-index:3;
}

@keyframes confettiFall{
  to {
    transform: translateY(110vh) rotate(360deg);
    opacity: 0;
  }
}
</style>
</head>
<body>

<!-- Music -->
<audio id="bgMusic" loop>
  <source src="music.mp3" type="audio/mpeg">
</audio>

<!-- PAGE 1 -->
<div class="card page active" id="page1">
  <div class="valentine-title">
    💖 DILNA JOY 💖<br>
    Will You Be Mine Valentine? 💌
  </div>

  <p>Be honest… you smiled when you saw this, right? 😏💖</p>
  <p>Go on… click Yes and make my heart happy 🥹❤️</p>

  <div id="btnBox" style="position:relative; height:160px;">
    <button class="btn" onclick="yesClicked()">Yes 😍</button>
    <button class="btn no-btn" id="noBtn">No 🙈</button>
  </div>
</div>

<!-- PAGE 2 -->
<div class="card page" id="page2">
  <h1>I'm Sad 😢</h1>
  <p>I was hoping you’d choose me… not gonna lie 😔</p>
  <p>It hurts because I actually care 💔</p>
  <p>You make my heart do stupid things 🥺</p>
  <p>Even now, a part of me is still smiling for you 💕</p>

  <button class="btn" onclick="goBack()">Go Back 😔</button>
</div>

<!-- PAGE 3 -->
<div class="card page" id="page3">
  <h1>YAY!!! 😍💖</h1>
  <p>You said YES 💓</p>

  <p>Dilna Joy, you have no idea how attractive your smile is to me 😍</p>
  <p>Every time I talk to you, I forget about everything else 💭💖</p>
  <p>You don’t even try, and still you make my heart beat faster 💓</p>
  <p>I think my favorite place is wherever you are 🥰</p>
  <p>If I had one wish, it would be more moments with you 💫</p>
  <p>You make my heart feel calm, happy, and complete 💕</p>
  <p>Honestly… you’re a little too special to me ❤️</p>

  <p style="margin-top:15px;"><b>Happy Valentine’s Day, Dilna Joy 💌</b></p>
  <p><b>- Sagar ❤️</b></p>
</div>

<script>
// Floating hearts
setInterval(() => {
  const heart = document.createElement("div");
  heart.className = "heart";
  heart.innerText = "💗";
  heart.style.left = Math.random() * 100 + "vw";
  heart.style.fontSize = (16 + Math.random()*20) + "px";
  document.body.appendChild(heart);
  setTimeout(() => heart.remove(), 6000);
}, 400);

// Confetti
function launchConfetti(){
  for(let i=0;i<80;i++){
    const conf = document.createElement("div");
    conf.className = "confetti";
    conf.style.left = Math.random() * 100 + "vw";
    conf.style.background = ["#ff5c8a","#ffd1dc","#ff9acb","#ff3b7a"][Math.floor(Math.random()*4)];
    conf.style.animationDuration = (2 + Math.random()*2) + "s";
    document.body.appendChild(conf);
    setTimeout(() => conf.remove(), 4000);
  }
}

// No button run (fixed)
var noBtn = document.getElementById("noBtn");
var btnBox = document.getElementById("btnBox");

noBtn.addEventListener("mouseenter", moveNoButton);
noBtn.addEventListener("touchstart", moveNoButton);

function moveNoButton(){
  var maxX = btnBox.clientWidth - noBtn.offsetWidth;
  var maxY = btnBox.clientHeight - noBtn.offsetHeight;

  var randomX = Math.random() * maxX;
  var randomY = Math.random() * maxY;

  noBtn.style.left = randomX + "px";
  noBtn.style.top = randomY + "px";
}

// Page logic
function yesClicked(){
  document.getElementById("bgMusic").play();
  launchConfetti();
  showPage("page3");
}

function goBack(){
  showPage("page1");
}

function showPage(id){
  var pages = document.getElementsByClassName("page");
  for(var i=0;i<pages.length;i++){
    pages[i].classList.remove("active");
  }
  document.getElementById(id).classList.add("active");
}
</script>

</body>
</html>
